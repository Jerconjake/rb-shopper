services:
  - type: web
    name: dw-botanicals-advisor
    env: python
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: gunicorn server:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
