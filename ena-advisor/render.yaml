services:
  - type: web
    name: ena-advisor
    runtime: python
    rootDir: ena-advisor
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
