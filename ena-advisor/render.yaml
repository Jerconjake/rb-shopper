services:
  - type: web
    name: ena-advisor
    runtime: python
    rootDir: ena-advisor
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
