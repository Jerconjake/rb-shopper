services:
  - type: web
    name: rb-shopper
    runtime: python
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: python server.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 10000
