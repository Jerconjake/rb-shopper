services:
  - type: web
    name: tsp-advisor
    runtime: python
    rootDir: tsp-advisor
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
